class Symbol:

    def __init__(self, symbol_id, symbol_type_id, symbol, name, full_name, meta):
        self.symbol_id = symbol_id
        self.symbol_type_id = symbol_type_id
        self.symbol = symbol
        self.name = name
        self.full_name = full_name
        self.meta = meta
